import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";

const initialState = {
  professional:null
}

const PorfessionalSlice = createSlice({
  name:"professional",
  initialState,
  reducers: {
    
  }
})